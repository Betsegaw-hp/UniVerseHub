<nav class="navbar navbar-expand-lg navbar-light bg-light bg-gradient sticky-top bg-body-tertiary ">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="bi bi-globe"></i> UniVerseHub
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/forum"><i class="bi bi-chat-dots"></i> Forum</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#"><i class="bi bi-camera-video"></i> Videos</a>
                </li>
            </ul>
            <form class="d-flex me-2">
                <input class="form-control me-1" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit"><i class="bi bi-search"></i></button>
                <!-- https://getbootstrap.com/docs/5.3/forms/input-group/#segmented-buttons (for ref) -->
            </form>
            <div class="dropdown">
                <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
                    <% if(user) { %>
                        <img src="<%= user.avatarUrl %>" alt="avatar" class="avatar-sm" /> welcome, <span class="capitalize"><%= user.name %></span>
                    <% } else { %>
                        <!-- this shouldn't happen. if happens auth messed up -->
                        <i class="bi bi-person-circle"></i> Welcome, stranger?
                    <% } %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuLink">
                    <li><a class="dropdown-item" href="/profile/<%= user?.username || "404" %>">Profile</a></li>
                    <li><a class="dropdown-item" href="/profile/setting">Settings</a></li>
                    <% if(user.role === 'admin') {%>
                        <li><a class="dropdown-item" href="/admin">Admin Dashboard</a></li>
                    <% } %>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/auth/logout">Log out</a></li>
                </ul>
            </div>
        </div>
    </div>
</nav>